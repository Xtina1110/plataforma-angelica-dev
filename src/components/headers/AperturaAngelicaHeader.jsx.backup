/**
 * Header de Apertura Angelical
 * Usa el componente unificado con tema purple
 */

import React from 'react';
import UnifiedAngelicalHeader from './UnifiedAngelicalHeader';

const AperturaAngelicaHeader = (props) => {
  return (
    <UnifiedAngelicalHeader
      app="apertura"
      {...props}
    />
  );
};

export default AperturaAngelicaHeader;

